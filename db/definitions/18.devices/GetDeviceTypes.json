{
    "schema": {
        "type": "database",
        "version": "1.0.0"
    },
    "definition": {
        "mode": "sp",
        "name": "GetDeviceTypes",
        "inputs": [
            { "name": "langId", "sqlType": "nvarchar(3)", "default": null },
            { "name": "deviceTypeId", "sqlType": "int", "default": null }
        ]
    }    
}
